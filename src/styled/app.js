import styled from 'styled-components';

const App = styled.div`
  font-family: Arial, Helvetica, sans-serif;

  > button {
    width: 100px;
    height: 30px;
    margin: 30px auto 0;
    cursor: pointer;
    border-radius: 5px;
    display: block;
  }
`;

export default App;